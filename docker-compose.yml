version: '3.8'
services:
  postgres:
    image: postgres:15
    container_name: workload_db
    environment:
      POSTGRES_DB: workloadplanner
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    ports:
      - "5432:5432"